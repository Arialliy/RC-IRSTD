numpy>=1.23
pandas>=1.5
pillow>=9.0
scipy>=1.9
scikit-image>=0.20
torch>=2.0
torchvision>=0.15
tqdm>=4.64

# Test-only; kept here because the repository does not yet publish a package
# with optional dependency groups.
pytest>=7.0
