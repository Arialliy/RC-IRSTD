# Legacy engineering documents

本目录保存 0.3.x 快照中的历史工程文档，仅用于追溯旧实现。

其中可能包含以下已从论文主路径移除或降级为基线的设计：

- direct threshold regression；
- Reject/abstention head；
- 双 pixel/component 风险曲线；
- CRC/conformal 扩展；
- 旧式 Tail-CVaR + Miss-CVaR 独立模块。

AAAI-27 定稿方法以仓库根目录 `README.md`、`docs/AAAI27_TWO_STAGE_NO_REJECT_FINAL_PLAN.md`、
`docs/EXPERIMENT_PROTOCOL.md` 和 `docs/TRAINING_RUNBOOK.md` 为准。
